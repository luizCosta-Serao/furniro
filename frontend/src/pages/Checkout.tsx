import HeroCheckout from '../components/Checkout/HeroCheckout'
import PurchasedProducts from '../components/Checkout/PurchasedProducts'

const Checkout = () => {
  return (
    <main>
      <HeroCheckout />
      <PurchasedProducts />
    </main>
  )
}

export default Checkout